import ProfileForm from './ProfileForm';
import {
  Center,

} from '@chakra-ui/react';

const UserProfile = () => {
  return (
    <Center>
      <ProfileForm />
    </Center>
  );
};

export default UserProfile;
